CONSTANTS
    Jobs = {j1, j2, j3}
    Unassigned = {j3}
    NoJob = none

SPECIFICATION Spec

\* Only NoLostSession, so the counterexample TLC prints is precisely the
\* lost update -- one session's config block destroyed by another's write.
INVARIANTS
    NoLostSession
